import { Typography } from 'antd'

const { Title, Text } = Typography

export function TimelinePage() {
  return (
    <div>
      <Title level={3}>时间轴视图</Title>
      <Text type="secondary">每日时间安排（开发中...）</Text>
    </div>
  )
}
